version: v1.0
name: Initial Pipeline
agent:
  machine:
    type: e1-standard-2
    os_image: ubuntu2004
queue:
  - when: pull_request =~ '^PR_.'
    name: pullrequest-$SEMAPHORE_GIT_PR_NUMBER
blocks:
  - name: 'Block #1'
    task:
      jobs:
        - name: 'Job #1'
          commands:
            - checkout
            - sleep 10
